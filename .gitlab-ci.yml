image: mcr.microsoft.com/playwright:v1.54.2-noble

workflow:
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "dev"

stages:
  - prepare
  - install
  - test

variables:
  npm_config_cache: "$CI_PROJECT_DIR/.npm"
  APP_BASE_URL: "http://localhost:3000"
  API_BASE_URL: "http://localhost:3000"

cache:
  - key: "test-deps-$CI_COMMIT_REF_SLUG"
    paths:
      - .npm/
      - node_modules/
    policy: pull-push
  - key: "app-$CI_COMMIT_REF_SLUG"
    paths:
      - /tmp/juice-shop/node_modules/
    policy: pull-push

prepare_app_for_testing:
  stage: prepare
  image: node:22
  script:
    - echo "Start preparing app for testing..."
    - git clone https://github.com/niks-xz/my-juice-shop.git /tmp/juice-shop
    - cd /tmp/juice-shop
    - npm install
    - npm run start && echo "App is running on $APP_BASE_URL"

install_dependencies:
  stage: install
  script:
    - npm ci
  artifacts:
    paths:
      - node_modules/
    expire_in: 1 hour
  dependencies:
    - prepare_app_for_testing

run_tests:
  stage: test
  script:
    - npm run test
  artifacts:
    when: always
    paths:
      - allure-results/
      - test-results/
      - playwright-report/
    expire_in: 1 hour
  dependencies:
    - prepare_app_for_testing
    - install_dependencies
